Options -MultiViews
RewriteEngine On

# Serve real assets and PHP API endpoints directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send client-side routes to the React application.
RewriteRule ^ index.html [L]
